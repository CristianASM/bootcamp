		Identificacion
	[git config --global user.name "Cristian Soto"]
	[git config --global user.email cristiansoto.m10@gmail.com]
	[git config --list] para ver todas las configuraciones guardadas.


		Crear repositorio 
	Con comando [cd] se viaja entre carpetas.
	Con comando [ls] muestra las carpetas y archivos.
	Con comando [mkdir] se crea una carpeta.
	Con comando [touch] crea un archivo y se le puede añadir su extension, ej: texto.txt.
	Con comando [git init] para iniciar un repositorio local.
	Con comando [git remote add origin (enlace del repositorio remoto github)] para conectar el repositorio local con el remoto.
	Con comando [git commit -m "(comentario)"] confirma los cambios y puedes agregar un comentario. 
	Con comando [git push] sube el archivo local al remoto.
	Con comando [git pull] baja los archivos remotos al local.
	Con comando [git fetch] muestra los cambios que se hicieron en el repositorio remoto.
	Con comando [git branch] muestra la branch en la que uno se encuentra y tambien se puede usar para crear una rama nueva.
	Con comando [git switch] se cambia de rama.
	Con comando [git status] muestra el estado en el que se encuentran tus archivos.
	Con comando [git log] muestra los cambios que se han hecho. 
	Esto son los comandos basicos para iniciar un repositorio local y sincronizarlos con un remoto.


	Con comando [git diff] muestra los cambios exactos que se hicieron en un archivo.
	Con comando [git mv (nombre-archivo nuevo-nombre)] cambia el nombre de un archivo o carpeta.
	Con [git log --pretty=format:"%h %an %s"] esto muestra el log mas ordenado .. agregandole {--graph} muestra los merge.
	Con comando [git rm (nombre del archivo)] eliminas un archivo.  

		
		Clonar un repositorio
	Con comando [git clone (Url)] puedes clonar un repositorio usando HTTPS o SSH. Ej: $git clone https://github.com/CristianASM/???.git